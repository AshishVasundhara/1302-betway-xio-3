
using UnityEngine;
/*
public class AdbuddizLoader : MonoBehaviour
{
	private static int proposalCount;

	private static int adSeen;

	private static float nextAdTime;

	private void Start()
	{
		AdinCube.SetAndroidAppKey("daad38a5a3c544238505");
		AdinCube.Interstitial.Init();
		AdinCube.Rewarded.Fetch();
	}

	private void Update()
	{
	}

	public static void ProposeAdDisplay()
	{
		if (!Purchaser.HasRemovedAds() && Time.time > nextAdTime && adSeen < 4)
		{
			nextAdTime = Time.time + 90f;
			adSeen++;
			AdinCube.Interstitial.Show();
			//FirebaseAnalytics.LogEvent("ad_displayed_adincube", "ok", 1.0);
		}
	}
}*/
